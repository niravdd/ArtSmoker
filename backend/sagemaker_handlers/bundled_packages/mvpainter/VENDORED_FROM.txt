Vendored from https://github.com/amap-cvlab/MV-Painter
Commit: f714682db5e6f78e8d1c7c0a43d1525638dda5a6
Date: 2026-06-11
Paper: MVPainter (arXiv:2505.12635)
Model weights: https://huggingface.co/shaomq/MVPainter (Apache-2.0)

NOTE: MVPainter's companion IDArb (lizb6626/IDArb) was evaluated for PBR map
generation and RULED OUT — its published WEIGHTS carry NO license tag
("[More Information Needed]"), so they are not commercial-safe (MIT covers the
CODE only). For normal maps we instead use StableNormal (Stable-X/
yoso-normal-v1-8-1, Apache-2.0 weights AND code) — see inference.py
_load_normal_pipe / _estimate_normal. Do NOT reintroduce IDArb unless its
weights are explicitly relicensed.

WHY ONLY A SUBSET IS VENDORED — LICENSE SPLIT
─────────────────────────────────────────────────────────────────────────────
MVPainter's repo is TWO separable halves with DIFFERENT licenses:

  1. Multi-view DIFFUSION (Apache-2.0, commercial-safe) — the actual MVPainter
     contribution. SDXL multi-view UNet + Union ControlNet geometry conditioning.
     This is what we vendor.

  2. The Stage-3 BAKE (infer_paint.py, mvpainter/bake_pipeline*.py,
     mvpainter/custom_rasterizer/, mvpainter/differentiable_renderer/,
     mvpainter/utils.py, scripts/blender_*.py) — vendored Tencent Hunyuan3D code
     under the TENCENT HUNYUAN NON-COMMERCIAL LICENSE, plus bpy (GPLv3).
     *** NOT COMMERCIAL-SAFE — DELIBERATELY EXCLUDED. ***

We use MVPainter's commercial-safe diffusion ONLY, and replace its Stage-3 bake
with ArtSmoker's existing nvdiffrast back-projection baker (the same path used
by the MV-Adapter backend). MVPainter's diffusion emits 6 RGB views at known
orthographic camera poses; those drop straight into our baker, so the
non-commercial bake code is never needed.

FILES VENDORED (all Apache-2.0, verified — no non-commercial headers, no
imports of the excluded modules):
  mvpainter/mvpainter_pipeline.py  — MVPainter_Pipeline, RefOnlyNoisedUNet,
                                     DepthControlUNet (self-contained; imports
                                     only diffusers/transformers/torchvision;
                                     defines its own to_rgb_image).
  mvpainter/controlnet.py          — ControlNetModel_Union (Apache, HuggingFace-
                                     derived; no internal MVPainter imports).
  mvpainter/__init__.py            — empty package marker.
  LICENSE                          — upstream Apache-2.0 + NOTICE.

DELIBERATELY NOT VENDORED:
  mvpainter/utils.py               — TENCENT HUNYUAN NON-COMMERCIAL header.
  mvpainter/bake_pipeline*.py      — Stage-3 bake (Hunyuan/GPL).
  mvpainter/custom_rasterizer/     — Hunyuan CUDA rasterizer (non-commercial).
  mvpainter/differentiable_renderer/ — Hunyuan renderer (non-commercial).
  infer_paint.py, scripts/blender_* — Blender/bpy (GPLv3) bake assembly.
  model_unet.py, model_controlnet.py — training-only LightningModules (unused
                                     at inference).
  train*.py, evaluation/, data_process/ — training/eval (not needed).

INFERENCE INVOCATION (mirrored in inference.py::_generate_texture_mvpainter):
  pipe = MVPainter_Pipeline.from_pretrained('shaomq/MVPainter', torch_dtype=fp16)
  cn   = ControlNetModel_Union.from_unet(pipe.unet)
  pipe.add_controlnet(cn, conditioning_scale=1.0)
  ckpt = torch.load(hf_hub_download('shaomq/MVPainter',
                    'unet_w_controlnet/v29_25000.ckpt'),
                    map_location='cpu')['state_dict']   # CPU-map (issue #13 VRAM)
  pipe.unet.load_state_dict({k[5:]: v for k,v in ckpt.items() if 'unet' in k})
  out = pipe(ref_image, depth_image=normal_tile, depth_image_2=depth_tile,
             num_inference_steps=75)
  # out[0] = 3x2 grid of 6 RGB views (512*2 wide x 512*3 tall) → our baker.

ORIENTATION: for TripoSG meshes use azimuth offset 0 (README-authoritative;
the infer_multiview.py default -90 is for Hunyuan/TRELLIS/Hi3DGen). Use the
SAME offset for the conditioning render and the back-projection bake.

DIFFUSERS COMPATIBILITY SHIM (ArtSmoker modification to controlnet.py)
─────────────────────────────────────────────────────────────────────────────
Upstream pins diffusers==0.20.2, but ArtSmoker's container runs diffusers>=0.30
(required by TripoSG / MV-Adapter / Hunyuan). Rather than isolate MVPainter in a
separate venv, we patched THREE import lines in controlnet.py to the modern
diffusers layout (mvpainter_pipeline.py needed NO changes):

  - from diffusers.models.unet_2d_blocks      → diffusers.models.unets.unet_2d_blocks
  - from diffusers.models.unet_2d_condition   → diffusers.models.unets.unet_2d_condition
    (both relocated under models.unets/ in diffusers 0.26; old paths removed 0.29)
  - FromOriginalControlnetMixin (removed in 0.29) dropped from imports AND the
    ControlNetModel_Union base classes — it is a from_single_file loader mixin
    NEVER exercised at inference (weights load via ModelMixin.from_pretrained /
    from_unet). All changes marked with "ARTSMOKER" comments in controlnet.py.

Verified safe against diffusers source at tags 0.20→0.34: the relocated block
classes + get_down_block are byte-for-byte identical (no new required args; all
call sites keyword-only), and the attention-processor / forward interfaces only
GAINED optional params. Original upstream pins (for reference): diffusers==0.20.2,
huggingface-hub==0.22.2 (the latter for issue #12 vision_encoder load — we use
huggingface-hub>=0.20 and load both CLIP vision encoders via from_pretrained).

VALIDATION TODO at first deploy: load the v29_25000.ckpt and confirm zero
unexpected_keys / missing_keys against the reconstructed ControlNet (the one
thing not verifiable from source alone).
