# Requirements for the SageMaker inference container.
# These are installed INSIDE the container alongside the model weights.
# The universal inference handler (inference.py) imports from these.
#
# The DLC container has pre-installed versions. We pin upper bounds to
# avoid breaking the CUDA-compiled torch and to ensure reproducibility.

# Core — DO NOT upgrade torch (DLC has CUDA-compiled version)
torch>=2.6.0,<2.7.0
torchvision>=0.21.0,<0.22.0
Pillow>=10.0
numpy>=1.24,<2.5

# HuggingFace ecosystem — pinned ranges for compatibility
diffusers>=0.36.0,<0.38.0
transformers>=4.51.0,<4.58.0
accelerate>=1.6.0,<2.0.0
safetensors>=0.4.3
peft>=0.17.0,<0.19.0
huggingface-hub>=0.34.0,<1.0.0

# For Real-ESRGAN upscaling (install deps but skip tb-nightly conflict)
realesrgan==0.3.0
basicsr==1.4.2

# For image processing (headless — no GUI libs needed in container)
opencv-python-headless>=4.8.0
