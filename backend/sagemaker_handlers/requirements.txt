# Requirements for the SageMaker inference container.
# These are installed INSIDE the container alongside the model weights.
# The universal inference handler (inference.py) imports from these.

# Core (always needed)
torch>=2.1.0
Pillow>=10.0
numpy>=1.24

# Faster HuggingFace downloads (Xet parallel chunked transfer)
hf_xet>=1.0

# For diffusers-based models (FLUX, SDXL, SVD, ControlNet)
diffusers>=0.36.0
transformers>=4.47.0
accelerate>=0.34.0
safetensors>=0.4.0
peft>=0.17.0
huggingface-hub>=0.34.0

# For Real-ESRGAN upscaling
realesrgan>=0.3.0
basicsr>=1.4.2

# For image processing
opencv-python-headless>=4.8.0
torchvision>=0.16.0
